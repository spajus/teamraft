<!DOCTYPE html>
<html>
  <%= render 'shared/head' %>
  <body class="application">
    <div class="container">
      <%= render 'shared/nav' %>
      <%= render 'shared/alerts' unless @bulk_update_person_id %>
      <%= yield %>
      <%= render 'shared/footer' %>
    </div>
  </body>
</html>
