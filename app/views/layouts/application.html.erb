<!DOCTYPE html>
<html>
  <%= render 'shared/head' %>
  <body class="application">
    <div class="container">
      <%= render 'shared/nav' %>
      <% if notice %><p class="notice"><%= notice %></p><% end %>
      <% if alert %><p class="alert"><%= alert %></p><% end %>
      <%= yield %>
    </div>
  </body>
</html>
